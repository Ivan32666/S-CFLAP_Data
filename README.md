# S-CFLAP_Data
Data for S-CFLAP

Each instance contains the number and coordinate information of the customer nodes and candidate facilities. The first line of each data file specifies the number of customer nodes (N) and candidate facilities (M). Starting from the second line, the next N lines provide the coordinates of the customer nodes, followed by M lines containing the coordinates of the candidate facilities.
